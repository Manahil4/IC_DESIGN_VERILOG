I2C; Inter Integrated Circuit Bus Protocol
by just 2 lines of I2c protocol, 128 devices could be connected

serial data line
serial clock line

these 2 lines should be pulled up by 5 v(BUT NOT COMPULSORY), because many boards have already internal pullups available
and it will make these 2 lines active high

multi master maulti slave support
 
address =7b, so 128 devices could be connected

master initiates data transfer, and control clk, sync clk???

CLK STRETCHING
if slave lowers SCL; so M can't transfer data so it acts like a gate for slave to accept or reject data;

 
both SDA, SCL are high, but when M sends data
for START bit; sda, scl both goes low


M_S_write_req

M-> S ; initiate data transmit
M; should mention address of Slave, on SDA with clk, as many slaves are on line, but only highlighted slave will get to know 

M; master then send write bit=0, for write signal to slave
S; send 0 for ack if it's free else 1 if slave is unavailable

M; sends data(8b)
S; again sends ack by 0 for showing it gets data



M_ S_ read_req
M; start bit
M; slave's address
S; identify that some M wants a connection with it
M; sends Read signal; Read b=1
S; gives ack 0
S; now it sends data (8b) that it store/ have, to M
M; sends ack=1( M gives 1 as ack while slave 0 for ack)
M; sends stop bit =0